import { DescDirectiveDirective } from './desc-directive.directive';

describe('DescDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new DescDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});

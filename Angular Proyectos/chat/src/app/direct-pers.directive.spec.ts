import { DirectPersDirective } from './direct-pers.directive';

describe('DirectPersDirective', () => {
  it('should create an instance', () => {
    const directive = new DirectPersDirective();
    expect(directive).toBeTruthy();
  });
});

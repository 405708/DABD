import { CleanChatDirective } from './clean-chat.directive';

describe('CleanChatDirective', () => {
  it('should create an instance', () => {
    const directive = new CleanChatDirective();
    expect(directive).toBeTruthy();
  });
});
